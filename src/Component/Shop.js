import React, {useState, useEffect} from 'react'
import '../App.css'

function Shop(){
 
  return(
    // <div>
    //    {items.map(item => (
    //        <h1>{item.name}</h1>
    //    ))}
    // </div>
    <div className="header">
      <h1>Shoping Page</h1>
    </div>
  );
}

export default Shop;
